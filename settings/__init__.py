# column names which actually represent the amount, price ie. cost_basis, gain, etc
PORTFOLIO_TABLE_AMOUNT_COLUMN_NAMES = ["price", "cost_basis", "market_value", "price_paid", "gain"]
# list of column names to hide
PORTFOLIO_TABLE_COLUMN_NAMES_TO_HIDE = ["id", "symbol", "currency"]
# column names to sum up
PORTFOLIO_TABLE_SUMUP_COLUMN_NAMES = ["cost_basis", "market_value", "gain"]
